## Packages
react-hook-form | Form state management
@hookform/resolvers | Zod integration for react-hook-form
recharts | Visualization for the dashboard metrics
date-fns | Date formatting and manipulation
framer-motion | Smooth animations for modal and list transitions
lucide-react | Beautiful icons (already in base but essential)
clsx | Conditional class names
tailwind-merge | Merging tailwind classes

## Notes
API endpoints defined in shared/routes.ts
Images are uploaded via POST /api/upload -> { url: string }
Work logs are stored via POST /api/logs
